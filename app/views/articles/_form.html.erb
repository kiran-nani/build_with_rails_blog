<% if @article.errors.any? %>
  <div class="errors">  
    <h3>
      <%= pluralize(@article.errors.count, "error") %> prohibited this from being saved
    </h3>
    <ul>
      <% @article.errors.full_messages.each do |message| %>
        <li class="error"><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= form_for :article, url: articles_path do |f| %>
  <p>
    <%= f.label :title %><br/>
    <%= f.text_field :title %>
  </p>
  <p>
    <%= f.label :body %><br/>
    <%= f.text_area :body %>
  </p>
  <%= f.submit %>
<% end %>